This repository is primarily run inside a Docker environment.

Notes:

- The development servers for `view-user` and `view-admin` are intended to run in containers.
- When running commands, prefer using the project's Docker compose or containerized scripts when available.
- Paths in documentation may assume container context.
