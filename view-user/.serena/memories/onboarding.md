Onboarding for `view-user`:

1. Install dependencies (inside Docker container if applicable):
   - npm ci
2. Development:
   - npm run dev
3. Linting:
   - npm run lint
4. Build/Start:
   - npm run build && npm run start
5. GraphQL codegen (if schema or queries change):
   - npm run codegen
