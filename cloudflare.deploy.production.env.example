# Copy to .cloudflare.deploy.production.env with mode 600.
# Public coordinates are loaded only from cloudflare.project.env.
# Never use the shared Cloudflare account owner address in either named-user roster.
ADMIN_EMAILS='[]'
SCREEN_EMAILS='[]'
STAMP_DAILY_LIMIT=25000
