services:
  caddy:
    environment:
      CADDY_SITE_ADDRESS: ${PUBLIC_DOMAIN:?PUBLIC_DOMAIN is required for VPS direct publication}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
